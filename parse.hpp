#ifndef _PARSE_HPP
#define _PARSE_HPP

#include <string>
#include "param.hpp"
using namespace std;

//function to parse the command line
void parseCommandLine(const string &input, Param &params);

#endif